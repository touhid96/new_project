import React from "react";
import { useLoaderData } from "react-router-dom";

const Bookdetails = () => {
  const loadr = useLoaderData();
  return (
    <div>
      <p>Hi threre</p>
    </div>
  );
};

export default Bookdetails;
